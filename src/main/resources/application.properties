spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://${DATABASE_ADDRESS:localhost}:${DATABASE_PORT:3306}/${DATABASE_NAME:db_example}?createDatabaseIfNotExist=true&spring.datasource.url=jdbc:mysql://${DATABASE_ADDRESS:localhost}:${DATABASE_PORT:3306}/${DATABASE_NAME:db_example}?createDatabaseIfNotExist=true&spring.datasource.url=jdbc:mysql://${DATABASE_ADDRESS:localhost}:${DATABASE_PORT:3306}/${DATABASE_NAME:db_example}?createDatabaseIfNotExist=true&sslMode=REQUIRED
spring.datasource.username=${DATABASE_USERNAME:springuser}
spring.datasource.password=${DATABASE_PASSWORD:huanlin2022}
spring.datasource.driver-class-name =com.mysql.jdbc.Driver
logging.file.name=/var/log/spring.log
aws.sns.topic_arn=${AWS_SNS_TOPIC_ARN:arn:aws:sns:us-east-1:351248346419:createUser}
aws.region=${AWS_REGION: }
aws.bucket_name=${BUCKET_NAME: }
publish.metrics=true
metrics.server.hostname=localhost
metrics.server.port=8125
domain=${DOMAIN:localhost:8080}
